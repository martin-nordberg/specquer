{
  "name": "specquer",
  "type": "module",
  "private": true,
  "workspaces": [
    "server",
    "agent",
    "client",
    "shared",
    "documentation"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7"
  },
  "scripts": {
    "dev": "bun run --filter @specquer/server --filter @specquer/client dev",
    "typecheck": "tsc --noEmit && tsc --noEmit -p client"
  }
}
